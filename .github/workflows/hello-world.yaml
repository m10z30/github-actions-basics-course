name: Hello World

on:
    workflow_dispatch:

jobs:
    print-hello-world:
        runs-on: ubuntu-latest

        steps:
            - name: Hello World
              run: echo "hello world"
              